[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["k_bandits_env"]

[project]
name = "k_bandits_env"
version = "0.0.1"
dependencies = ["gymnasium"]
